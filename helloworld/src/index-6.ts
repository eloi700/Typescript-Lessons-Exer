// import { calculateTax } from './tax';

// let tax = calculateTax(10_000);
// console.log(tax);

import * as _ from 'lodash';

_.clone([1, 2, 3])
// install lodash - npm i lodash
// install dev-dependency - npm i --save-dev @types/lodash OR -D @types/lodash
// Dev-Dependency not for DEPLOYMENTS instead for BUILD time
// lodash - pure JS library, it doesn't have JSDoc comment & declaration files.  Use Definitelytyped from GitHub for different declaration files library