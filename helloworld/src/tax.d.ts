// Note:  the name of the this file must be the same on its corresponding JS file but the extension is .d.ts - d is short for decoration

// Declare all the features for the target module
export declare function calculateTax(income: number): number;